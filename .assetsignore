# Files that must not be published as part of the site.
.git
.gitignore
.assetsignore
wrangler.jsonc
node_modules
README.md
# The React app source (built output is deployed separately when we flip).
web
